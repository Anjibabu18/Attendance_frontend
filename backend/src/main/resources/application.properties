server.port=${PORT:8081}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/attendance?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Kolkata}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:2395}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Kolkata

spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

app.jwt.secret=${JWT_SECRET:attendance-local-dev-secret-2026-secure-key}
app.jwt.issuer=attendance-app
app.jwt.expiresMinutes=${JWT_EXPIRES_MINUTES:10080}

app.cors.allowedOrigins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173}
app.security.loginMaxAttempts=${LOGIN_MAX_ATTEMPTS:5}
app.security.loginLockMinutes=${LOGIN_LOCK_MINUTES:5}
app.security.passwordMinLength=${PASSWORD_MIN_LENGTH:10}
app.security.passwordRequireMixedCase=${PASSWORD_REQUIRE_MIXED_CASE:true}
app.security.passwordRequireDigit=${PASSWORD_REQUIRE_DIGIT:true}
app.security.passwordRequireSpecial=${PASSWORD_REQUIRE_SPECIAL:true}

app.attendance.minDailyMinutes=${MIN_DAILY_MINUTES:480}
app.attendance.defaultJoinDate=${DEFAULT_JOIN_DATE:2026-01-19}

app.cloudinary.cloudName=${CLOUDINARY_CLOUD_NAME:ddm1jkelv}
app.cloudinary.apiKey=${CLOUDINARY_API_KEY:969539889143787}
app.cloudinary.apiSecret=${CLOUDINARY_API_SECRET:8ZgW-ymgd5hWTLoQ7i2l7S2cpe8}

app.face.serviceEnabled=${FACE_SERVICE_ENABLED:false}
app.face.serviceUrl=${FACE_SERVICE_URL:http://localhost:5055/verify}
app.face.timeoutMillis=${FACE_SERVICE_TIMEOUT_MILLIS:15000}
app.face.minScore=${FACE_MIN_SCORE:0.70}

# Optional: email notifications (leave requests, approvals, etc.)
# Set MAIL_ENABLED=true and configure SMTP to enable.
app.mail.enabled=${MAIL_ENABLED:true}
app.mail.from=${MAIL_FROM:anushatechnologies4@gmail.com}
# Optional override list; if empty, HR usernames that look like emails are used.
app.mail.hrRecipients=${MAIL_HR_RECIPIENTS:}

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USER:anushatechnologies4@gmail.com}
spring.mail.password=${MAIL_PASS:gefspacvgeeetsormу}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS:true}
