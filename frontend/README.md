# Attendance Frontend

Vite React frontend for the attendance system.

## Vercel deployment

Set this environment variable in Vercel:

- `VITE_API_URL=https://your-render-backend.onrender.com`

After the backend is deployed on Render, paste that Render URL into `VITE_API_URL` and redeploy the frontend.
